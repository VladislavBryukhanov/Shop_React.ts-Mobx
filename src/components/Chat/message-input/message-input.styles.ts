import { Theme, createStyles } from "@material-ui/core";

export const styles = (theme: Theme) => createStyles({
  messageInput: {
    width: '100%',
    height: 'fit-content'
  },
  textField: {
    flex: 1
  }
})