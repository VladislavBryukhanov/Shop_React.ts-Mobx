import { Theme, createStyles } from '@material-ui/core/styles';

export const styles = (theme: Theme) => createStyles({
  orderList: {
    paddingLeft: 64
  },
  orderDate: {
    textAlign: 'right'
  }
});
